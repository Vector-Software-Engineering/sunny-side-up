import styled from 'styled-components';

export const StyledImage = styled.div`

  img {
    position: relative;
    border-radius: 4px;
    padding: 5px;
    width: 50px;
    overflow: hidden:
  }

  div {
    height: 10px;
    width: 10px;
  }
`