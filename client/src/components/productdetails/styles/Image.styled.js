import styled from 'styled-components';

export const StyledImage = styled.div`

  img {
    position: relative;
    padding: 5px;
    width: 50px;
  }

  div {
    height: 3px;
    width: 3px;
    overflow: hidden:
  }
`