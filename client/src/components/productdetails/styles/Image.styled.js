import styled from 'styled-components';

const StyledImage = styled.div`
  img {
    padding: 1px;
    height: 50px;
    width: 50px;
  }

  button {
    all: revert;
  }
`;

export default StyledImage;
