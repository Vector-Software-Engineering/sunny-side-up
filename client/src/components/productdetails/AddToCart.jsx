import React from 'react';
//import Select from 'react-select';

const AddToCart = ({ currentStyle }) => {

  //console.log('current style inside of addToCart', currentStyle.skus);

  // for (let keys of currentStyle.skus) {
  //   console.log(keys, currentStyle.skus[keys]);
  // }

  return (
    <div>
      AddToCart Component
    </div>
  );
};

export default AddToCart;